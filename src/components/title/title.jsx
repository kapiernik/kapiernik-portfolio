import styled from "styled-components";

const Title = styled.h2`
    grid-area: title;
    text-align: left;
    margin-left: 50px;
    margin-top: 50px;
    font-size: 4rem;
    font-weight: 600;
`;

export default Title;